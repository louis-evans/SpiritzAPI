# SpiritzAPI
ExpressJS backend API called by Spiritz Vue.js frontend application.

Serves data from a local SQLite database. 

``` bash
# install dependencies
$ npm run install

# start the application
$ npm run start
```
